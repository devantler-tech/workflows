name: 🌈 Zizmor (Static Analysis for GitHub Actions)

on:
  workflow_call:
  ### Required Workflow Triggers ###
  pull_request:
  merge_group:
  ##################################

permissions: {}

jobs:
  zizmor:
    runs-on: ubuntu-latest
    # Ignore Require Workflow runs
    if: github.repository != 'devantler-tech/github-actions'
    permissions:
      security-events: write
      contents: read # only needed for private repos
      actions: read # only needed for private repos
    steps:
      - name: 📑 Checkout
        uses: actions/checkout@11bd71901bbe5b1630ceea73d27597364c9af683 # v4.2.2
        with:
          persist-credentials: false

      - name: 🌈 Run Zizmor
        uses: devantler-tech/github-actions/composite-actions/zizmor-action@d69de4b313f789d5aff0bf42614da212fc5362e2 # v1.8.4
